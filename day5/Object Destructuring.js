
//firstName is extracted and assigned to the variable firstName and lastName is assigned to the variable lastName opposite of array destructuring


const { lastName, firstName } = { 
    firstName: 'Hello', 
    lastName: 'World'
};

console.log(firstName);  // Output: 'Hello'
